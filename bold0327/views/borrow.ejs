<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>BLOG</title>
    <link rel="stylesheet" href="/css/style.css">
    <script src="/send_url.js"></script>
  </head>
  <body>
    <%- include('header'); %>
    <main>
      <ul class="list">
        <% users.forEach((user) => { %>
        <li>
          <% if (1) {%>
            <i>会員限定</i>
          <% } %>
          <h2><%= user.username %></h2>
          <p><%= user.email %></p>
        </li>
        <% }) %>
      </ul>
    </main>
  </body>
</html>
